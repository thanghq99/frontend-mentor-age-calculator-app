import { ComponentProps } from '@/types/routeType';
import React, { FC } from 'react';

const DetailPage: FC<ComponentProps> = () => {
  return <div className='text-base'>DetailPage</div>;
};

export default DetailPage;
